org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m -XX:ReservedCodeCacheSize=128m -XX:+HeapDumpOnOutOfMemoryError
org.gradle.daemon=false
org.gradle.workers.max=1
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Matikan kompilasi incremental Kotlin: sumber plugin (pub cache, drive C:)
# dan proyek (drive D:) di root berbeda, membuat relativeTo gagal lintas-drive.
kotlin.incremental=false
